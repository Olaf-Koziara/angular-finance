export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  useMockAuth: true, // Set to false when real backend is available
  tokenKey: 'auth_token',
  refreshTokenKey: 'refresh_token'
};
